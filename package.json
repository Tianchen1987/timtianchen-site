{
  "name": "staying-afloat",
  "version": "1.0.0",
  "private": true,
  "description": "Staying Afloat — a personal blog (migrated from WordPress).",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "import": "python3 tools/import_wordpress.py"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.2"
  }
}
